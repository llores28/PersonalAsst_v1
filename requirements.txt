# Core
# openai-agents pin. Constraints:
#   - 0.3.x removed `function_tool(timeout=...)`; we already migrated.
#   - 0.4+ is needed to allow openai 2.x (browser-use 0.12 requires it).
#   - Cap at <1 in case the SDK ever ships a major-version refactor.
openai-agents>=0.4.0,<1
aiogram>=3.15.0
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Database
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
# psycopg2-binary 2.9.7 has no Python 3.12 wheel; 2.9.11 ships cp312 manylinux wheels
psycopg2-binary>=2.9.11
alembic>=1.14.0

# Memory
# Floor pinned to 2.x — older mem0ai versions pin langchain-community<0.3
# which conflicts with browser-use 0.12's transitive constraints; pip
# backtracks indefinitely through the 0.x line and fails to resolve.
# 2.x relaxes the langchain pins so the resolver completes cleanly.
mem0ai>=2.0.0,<3

# Vector store client (for direct Qdrant access if needed)
qdrant-client>=1.12.0

# Cache
redis[hiredis]>=5.2.0

# Scheduling
apscheduler>=4.0.0a5

# Web search (optional — Tavily)
# tavily-python>=0.5.0

# LinkedIn (unofficial Voyager API)
linkedin-api>=2.0.0

# Browser automation (Playwright)
playwright>=1.40.0

# LLM-driven autonomous browsing (uses Playwright Chromium already installed
# above). Wired as the `browser` skill in src/skills/browser.py — gated behind
# `browser_use_enabled=True` and an always-on approval check on writes.
# 0.7.x had a CDP-launch race that hung Chromium startup in headless Docker
# (chrome bound the debug port but the upstream wait_for_cdp_url never
# connected). Bumped to 0.12.x where the launch path is rewritten.
browser-use>=0.12.0,<0.14

# LLM-ready web scraping (uses Playwright under the hood)
crawl4ai>=0.8.0

# Retry / resilience
tenacity>=9.0.0

# Utilities
pyyaml>=6.0.2
watchdog>=6.0.0
httpx>=0.28.0

# Optional - Multi-LLM Provider Support
# Install these to enable additional LLM providers beyond OpenAI
# anthropic>=0.40.0    # For Claude support
# google-genai>=0.3.0  # For Gemini support

# Dev / Test (install separately or via pip install -r requirements-dev.txt)
# pytest>=8.3.0
# pytest-asyncio>=0.24.0
# pytest-cov>=6.0.0
# ruff>=0.8.0
# mypy>=1.13.0

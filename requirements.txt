# Core
openai-agents>=0.1.0
aiogram>=3.15.0
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Database
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
# psycopg2-binary 2.9.7 has no Python 3.12 wheel; 2.9.11 ships cp312 manylinux wheels
psycopg2-binary>=2.9.11
alembic>=1.14.0

# Memory
mem0ai>=0.1.0

# Vector store client (for direct Qdrant access if needed)
qdrant-client>=1.12.0

# Cache
redis[hiredis]>=5.2.0

# Scheduling
apscheduler>=4.0.0a5

# Web search (optional — Tavily)
# tavily-python>=0.5.0

# LinkedIn (unofficial Voyager API)
linkedin-api>=2.0.0

# Browser automation (Playwright)
playwright>=1.40.0

# LLM-ready web scraping (uses Playwright under the hood)
crawl4ai>=0.8.0

# Retry / resilience
tenacity>=9.0.0

# Utilities
pyyaml>=6.0.2
watchdog>=6.0.0
httpx>=0.28.0

# Optional - Multi-LLM Provider Support
# Install these to enable additional LLM providers beyond OpenAI
# anthropic>=0.40.0    # For Claude support
# google-genai>=0.3.0  # For Gemini support

# Dev / Test (install separately or via pip install -r requirements-dev.txt)
# pytest>=8.3.0
# pytest-asyncio>=0.24.0
# pytest-cov>=6.0.0
# ruff>=0.8.0
# mypy>=1.13.0

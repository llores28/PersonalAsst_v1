# Requirements for Atlas Dashboard API
# Core framework
fastapi
uvicorn[standard]
pydantic
pydantic-settings
sqlalchemy
alembic

# OpenAI Agents SDK (must match main requirements — MCPServerStreamableHttp added in 0.13+)
openai-agents>=0.13.0
openai

# Database
asyncpg
psycopg2-binary

# Redis (async quality scores + health check)
redis

# Retry/backoff — required transitively by src/memory/conversation (DLQ on
# session-compaction) and src/scheduler/maintenance (per-user retry on
# memory eviction). The dashboard's /api/health/scheduler endpoint imports
# get_redis from src/memory/conversation, which pulls tenacity at module load.
tenacity

# Telegram (for sending test notifications)
aiogram

# Rate limiting
slowapi>=0.1.9

# Utilities
python-multipart>=0.0.9
python-dotenv==1.0.0
